import 'package:flutter/material.dart';

class StateExtendImage extends StatelessWidget {
  const StateExtendImage({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
